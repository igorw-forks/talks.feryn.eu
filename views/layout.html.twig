<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Thijs Feryn's past and upcoming talks</title>
        <meta name="description" content="Thijs Feryn's past and upcoming talks">
        <meta name="author" content="Thijs Feryn">
        <meta name="viewport" content="width=device-width,initial-scale=1">
	    <link href="css/bootstrap.css" rel="stylesheet">
	    <link href="css/bootstrap-responsive.css" rel="stylesheet">
        <link href="css/prettify.css" rel="stylesheet">
    	<!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    	<!--[if lt IE 9]>
      	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    	<![endif]-->
    </head>
    <body>
    <div class="container">

      <!-- Main hero unit for a primary marketing message or call to action -->
      <div class="hero-unit">
        <h1>Check out my talks !</h1>
	<p>Hi, my name is <a href="http://twitter.com/ThijsFeryn" target="_blank">Thijs Feryn</a> and I love public speaking. As a professional speaker in the webhosting industry, I use this site to list past and upcoming talks.</p>
      </div>
      <div class="page-header">
          <h1>Upcoming talks <small>I currently have <strong>{{upcomingCount}}</strong> upcoming talks</small></h1>
      </div>
      <table class="table table-bordered table-condensed">
          <thead>
		<tr>
			<th>Date</th>
			<th>Talk</th>
			<th>Event</th>
			<th>Venue</th>
		</tr>
	  </thead>
	  <tbody>
      {% for item in upcoming %}
      <tr>
	      <td>{{item.date}}</td>
              {% if item.joindin|length > 0 %}
	      <td><a href="http://joind.in/talk/view/{{item.joindin}}" target="_blank">{{item.title}}</a></td>
	      {% else %}
	      <td>{{item.title}}</td>
	      {% endif%}
	      <td>
              	{% if item.name|length > 0 %}
              		{% if item.url|length > 0 %}
	      <a href="{{item.url}}" target="_blank">{{item.name}}</a></td>
	      		{% else %}
	      			{{item.name}}
	      		{% endif %}
	      	{% else %}
	      &nbsp;
	      	{% endif %}
	      </td>
              {% if item.venue|length > 0 and item.city|length>0 and item.country|length>0 %}
	      {% if item.venueUrl|length > 0 %}
	      <td><a href="{{item.venueUrl}}" target="_blank">{{item.venue}}</a> <em>@ <a href="http://maps.google.com/maps?q={{item.city}}" target="_blank">{{item.city}}</a> </em><img src="/img/flags/Flag-of-{{item.flag}}.png" title="{{item.country}}" width="20px" height="20px" /></em></td>
	      {% else %}
	      <td>{{item.venue}} @ <a href="http://maps.google.com/maps?q={{item.city}}" target="_blank">{{item.city}}</a> <em><img src="/img/flags/Flag-of-{{item.flag}}.png" title="{{item.country}}" width="20px" height="20px" /></em></td>
	      {% endif %}
              {% else %}
	      <td>&nbsp;</td>
              {% endif %}
	  </tr>
      {% endfor %}
	  </tbody>
      </table>
      <div class="page-header">
          <h1>Past talks <small>I've already done <strong>{{pastCount}}</strong> talks</small></h1>
      </div>
        <table class="table table-bordered table-condensed">
            <thead>
            <tr>
                <th>Date</th>
                <th>Talk</th>
                <th>Ratings</th>
                <th>Event</th>
                <th>Venue</th>
            </tr>
            </thead>
      {% for item in past %}
          <tr>
              <td>{{item.date}}</td>
              {% if item.slides|length > 0 %}
              <td>
				<a href="{{item.slides}}" target="_blank">{{item.title}}&nbsp;<i class="icon-file"></i></a>
				{% if item.video|length > 0 %}
				<a href="{{item.video}}" target="_blank"><i class="icon-facetime-video"></i></a>				
				{% endif %}
			  </td>
			  {% elseif item.video|length > 0 %}			
              <td>
				<a href="{{item.video}}" target="_blank">{{item.title}}&nbsp;<i class="icon-facetime-video"></i></a>
			  </td>			
              {% else %}
              <td>{{item.title}}</td>
              {% endif %}
              <td>
                  {% if item.joindin|length > 0 %}
                  <a href="http://joind.in/talk/view/{{item.joindin}}" target="_blank">
                      <img src="http://joind.in/inc/img/rating-{{item.rating}}.gif" />
                  </a>
                  {% endif %}
              </td>
              <td>
                  {% if item.name|length > 0 %}
                  {% if item.url|length > 0 %}
                  <a href="{{item.url}}" target="_blank">{{item.name}}</a></td>
              {% else %}
              {{item.name}}
              {% endif %}
              {% else %}
              &nbsp;
              {% endif %}
              </td>
              {% if item.venue|length > 0 and item.city|length>0 and item.country|length>0 %}
              {% if item.venueUrl|length > 0 %}
              <td><a href="{{item.venueUrl}}" target="_blank">{{item.venue}}</a> <em>@ <a href="http://maps.google.com/maps?q={{item.city}}" target="_blank">{{item.city}}</a> </em><img src="/img/flags/Flag-of-{{item.flag}}.png" title="{{item.country}}" width="20px" height="20px" /></em></td>
              {% else %}
              <td>{{item.venue}} @ <a href="http://maps.google.com/maps?q={{item.city}}" target="_blank">{{item.city}}</a> <em><img src="/img/flags/Flag-of-{{item.flag}}.png" title="{{item.country}}" width="20px" height="20px" /></em></td>
              {% endif %}
              {% else %}
              <td>&nbsp;</td>
              {% endif %}
          </tr>
      {% endfor %}
      </table>
      <hr>

      <footer>
        <p>&copy; Thijs Feryn</p>
      </footer>

    </div> <!-- /container -->
    <script src="/js/jquery.js"></script>
    <script src="/js/bootstrap-transition.js"></script>
    <script src="/js/bootstrap-alert.js"></script>
    <script src="/js/bootstrap-modal.js"></script>
    <script src="/js/bootstrap-dropdown.js"></script>
    <script src="/js/bootstrap-scrollspy.js"></script>
    <script src="/js/bootstrap-tab.js"></script>
    <script src="/js/bootstrap-tooltip.js"></script>
    <script src="/js/bootstrap-popover.js"></script>
    <script src="/js/bootstrap-button.js"></script>
    <script src="/js/bootstrap-collapse.js"></script>
    <script src="/js/bootstrap-carousel.js"></script>
    <script src="/js/bootstrap-typeahead.js"></script>
    </body>
</html>
